# Secrets / auth — NEVER commit these
config.yaml
auth.json
auth.json.new
browser.json
oauth.json

# Personal library data (generated by the pipeline)
data/*.json

# Caches (MusicBrainz, TheAudioDB, genres, YTM thumbnails)
*_cache.db
mb_cache.db
*.sqlite
*.sqlite3

# Python
__pycache__/
*.pyc
.venv/
venv/

# Claude Code (personal/local settings only; committed settings.json is fine)
.claude/settings.local.json

# OS
Thumbs.db
.DS_Store
